{
  "name": "five-a-side-manager-root",
  "version": "1.0.0",
  "description": "Root package.json for Render unified deployment",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  }
}
